{
  "routes": [
    {
      "method": "GET",
      "path": "/event-owners",
      "handler": "event-owners.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-owners/count",
      "handler": "event-owners.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-owners/:id",
      "handler": "event-owners.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/event-owners",
      "handler": "event-owners.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/event-owners/:id",
      "handler": "event-owners.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/event-owners/:id",
      "handler": "event-owners.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
