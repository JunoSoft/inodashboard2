{
  "routes": [
    {
      "method": "GET",
      "path": "/event-posts",
      "handler": "event-post.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-posts/count",
      "handler": "event-post.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-posts/:id",
      "handler": "event-post.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/event-posts",
      "handler": "event-post.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/event-posts/:id",
      "handler": "event-post.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/event-posts/:id",
      "handler": "event-post.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
